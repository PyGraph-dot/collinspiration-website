{
  "extends": [
    "next/core-web-vitals" // 'next/core-web-vitals' already includes TypeScript linting
  ],
  "rules": {
    // Add or override specific ESLint rules here if needed
    // For example, to adjust 'react/no-unescaped-entities' if it's too strict:
    // "react/no-unescaped-entities": "off",
    // To allow 'any' type in specific cases (use sparingly):
    // "@typescript-eslint/no-explicit-any": "off",
    // To allow unused variables (use sparingly, prefer _prefix):
    // "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }]
  }
}
